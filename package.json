{
  "name": "hardhat-project",
  "devDependencies": {
    "@nomicfoundation/hardhat-toolbox": "^5.0.0",
    "@nomiclabs/hardhat-vyper": "^3.0.8",
    "@sun-protocol/tron-studio": "0.0.5",
    "hardhat": "^2.22.19"
  },
  "scripts": {
		"start": "hardhat node",
    "prepare": "node ./.setup.js",
    "compile": "hardhat compile --verbose --force",
    "clean": "hardhat clean",
    "test": "hardhat test",
    "compile-tron": "hardhat compile --network tron --verbose --force",
		"deploy": "npx hardhat --network localhost deploy",
		"deploy-tron": "npx hardhat --network tron deploy"
  },
  "dependencies": {
    "ethers": "^6.13.5"
  }
}
